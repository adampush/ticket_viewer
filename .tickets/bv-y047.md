---
id: bv-y047
status: in_progress
deps: [bv-wcue]
links: []
created: 2026-02-22T01:48:45Z
type: task
priority: 1
assignee: Adam Push
parent: bv-jzv8
---
# Normalize robot payload keys and schema contracts to ticket-native names

## Context / Why

This is Workstream 2 from `docs/tkv-native-cleanup-plan.md`. Robot payload/schema contract naming must match the ticket-native public model so automation remains deterministic.

## Scope

### In Scope

- Rename bead-era robot payload keys to ticket-native keys.
- Update robot schema generation/fixtures to match emitted payloads.
- Update contract tests for payload/schema parity.

### Out of Scope

- CLI/help wording changes (Workstream 1).
- Internal terminology cleanup not affecting payload contracts (Workstream 3).

## Ticket Type & Granularity

- Type: Task
- Granularity target: 1-3 commits focused on robot output builders and schema contracts.

## Prerequisites & Dev State

- Upstream workstream `bv-wcue` complete.
- Approved plan and contracts matrix available.

## Assumptions

- Old payload keys are removed from active output (no compatibility shim).

## Open Questions

- None currently. Any discovered external consumer risk must be documented immediately with mitigation notes.

## Implementation Guidance

- Update robot output builders in `cmd/bv` and `pkg/ui`.
- Update schema emitters and `tests/e2e` contract fixtures.
- Inherit NFR constraints: preserve deterministic output for unchanged fields and avoid algorithmic behavior changes.

Likely files:

- `cmd/bv/*.go` robot output paths
- `pkg/ui/*` robot output builders
- `tests/e2e/*robot*` contract tests

## Acceptance Criteria

- Emitted robot payload keys are ticket-native.
- `tkv --robot-schema` matches emitted JSON payloads.
- Contract tests pass with no legacy payload key expectations in active paths.

## Deployment & State

- Breaking/Non-breaking: breaking for consumers of old payload key names.
- Migration requirements: release notes include explicit old->new key mapping.
- Safe to deploy independently: yes, after Workstream 1.

## Validation Plan

- `tkv --robot-schema`
- `go test ./tests/e2e/... -run 'Robot|Schema|Contract'`
- `go test ./cmd/bv/...`
- `go test ./pkg/ui/...`
- `go build ./...`

Expected evidence:

- Schema and payload parity evidence in PR notes.
- `tk add-note bv-y047 "validation: ..."` with key pass outputs.

## Dependencies

- Upstream: `bv-wcue`
- Downstream: `bv-ojjg`

## Artifacts

- Updated schema fixtures/contracts
- Old->new key mapping notes
- Validation outputs recorded in ticket notes

## Notes

**2026-02-22T02:20:07Z**

Started Workstream 2 schema/payload normalization. Completed first payload key migration slice: --robot-file-issues output now emits total_issues/open_issues/closed_issues (instead of total_beads/open_beads/closed_beads) and --robot-impact now emits affected_issues (instead of affected_beads). Updated e2e correlation tests accordingly. Validation: go test ./cmd/bv/... ./pkg/analysis/... ./pkg/agents/... ./tests/e2e -run 'Correlation|RobotHelp|RobotDocs|TTY|Suggest' passed; go build ./... && go vet ./... && go test ./... passed.
